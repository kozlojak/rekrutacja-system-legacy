package parser;

import to.Default_TO;

/**
 * Example ofservice to parse the AMGEntity instance.
 *
 *@author kozlojak
 */

public class DefaultParser implements Parser<Default_TO>{
    @Override
    public void parseMessage(Default_TO message) {
        //perform actions on realMessage
    }
}
